{
  "name": "vault-cluster-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for HashiCorp Vault cluster deployment automation on OpenShift/Kubernetes",
  "main": "dist/index.js",
  "type": "module",
  "bin": {
    "vault-cluster-mcp-server": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js",
    "prepare": "npm run build"
  },
  "keywords": [
    "mcp",
    "vault",
    "hashicorp",
    "kubernetes",
    "openshift",
    "deployment",
    "automation"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
